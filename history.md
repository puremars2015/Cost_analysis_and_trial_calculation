# BOM 成本分析與試算系統 - 更新歷史

## 2026-08-05

### 功能變更

- **原料料號展開為多欄位** (`b4babed`)
  - `fetch_bom_items`: 展開 `item_3` 陣列為個別欄位（原料1、原料2...）
  - `AppResult` 新增 `max_item_3_count` 欄位追蹤最大原料數
  - `build_report_columns`: 根據 `max_item_3_count` 動態生成欄位
  - 前端表格與 Excel 匯出皆使用動態欄位
  - 移除靜態 `REPORT_COLUMNS` 常量

### 行為變更

- 之前：所有原料料號集中在單一欄位，用逗號分隔
- 之後：根據實際原料數量展開為多個欄位（原料1、原料2...），最多顯示實際最大數量
